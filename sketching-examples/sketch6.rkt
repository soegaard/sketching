#lang sketching
11
;(define ball (load-image "amiga-ball-transparent.png"))

#;(define (setup)
  (size 600 400))

#;(define (draw)
  (background 128)
  (void)
  ;(image-mode 'center)
  ;(no-tint)
  ;(image ball 300 200)
  ;(tint 0 0 100 10)
  #;(image ball mouse-x mouse-y))
